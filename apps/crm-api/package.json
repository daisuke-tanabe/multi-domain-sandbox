{
  "name": "@sandbox/crm-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./test-support": "./src/test-support.ts"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --import tsx --watch src/main.ts",
    "start": "node --env-file-if-exists=.env --import tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@hono/zod-validator": "catalog:",
    "@sandbox/api-core": "workspace:*",
    "@sandbox/shared": "workspace:*",
    "hono": "catalog:",
    "pg": "catalog:",
    "tsx": "catalog:",
    "ulid": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
